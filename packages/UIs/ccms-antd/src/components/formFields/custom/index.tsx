import { CustomField } from 'ccms';
import { CustomFieldConfig } from "ccms/dist/src/components/formFields/custom";

export const PropsType = (props: CustomFieldConfig) => { };

export default class CustomFieldComponent extends CustomField {
}