---
name: 4-skip模板
route: /steps/skip
---
import { Playground, Props } from "docz";

# skip模板
CCMS表单提交模板
常用的提交表单项 <br/>

## 使用方式
- (type:skip) 直接获取参数进入下一步
- (type:skip) + (type:fetch) + (type:table)完成条件查询与展示



## 表单提交展示
import  { PropsType } from "./";
import {CCMS} from "../../index";

*****

## APIs- 模板默认值
field配置同from
```
{
  "type": "skip",
  "layout": "horizontal",
  "fields":[]
}

```
## APIs- 模板默认值

 <Props of={PropsType} />
