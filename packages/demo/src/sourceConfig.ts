import { CCMSConfig } from "ccms/dist/main"

const sourceConfig: CCMSConfig = {
  steps: [
    {
      type: 'header',
      title: {
        statement: '示例页面',
        params: []
      }
    }
  ]
}

export default sourceConfig